export enum status {
	Idle,
	Loading,
	Failed,
	Success,
}

export enum MenuItemT {
	Lost = 'Lost',
	NotFollowingBack = 'Not Following Back',
	Mutual = 'Mutual',
	Following = 'Following',
	Followers = 'Followers',
}

export enum APIErrors {
	TfaFail,
	TfaNeeded,
	LoginFail,
}

export enum Strings {
	sid = 'sid',
	Reset = 'reset',
}
