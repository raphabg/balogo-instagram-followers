{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "msedge",
            "request": "launch",
            "name": "Debug SPA",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}",
        }
    ]
}